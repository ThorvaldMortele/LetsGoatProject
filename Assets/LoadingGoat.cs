using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadingGoat : MonoBehaviour
{
    //dont delete this, its used to find the goat loading icon, for some reason gameobject.find doesnt work if the obj is in dont destroy on load
    //it should work tho but we have not much time left and this seemed to work, ill update it later
}
