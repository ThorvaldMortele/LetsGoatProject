using System.Collections;
using System.Collections.Generic;
using Fusion;
using UnityEngine;
using UnityEngine.UI;

public class AssignButton : MonoBehaviour
{

    //void Start()
    //{
    //    GameLauncher spawner = FindObjectOfType<GameLauncher>();

    //    GetComponent<Button>().onClick.AddListener(delegate { spawner.BeginGame(GameMode.AutoHostOrClient); });
    //}
}
